easy
- add directory history to o alias so it works more than once
	- would also be useful then to have an i alias which moves toward the top of the history
- better way to manage bash command history
- nitrogen -> feh for background, which sets up on initial build
- Run fontsetup.sh automaticall IF you don't already have the font installed/configured
- SCDIR env variable instead of hardcoding ~/Pictures

medium
- bfs early exit if not -f
- bfs -a to work more like find for simple cases
		- "bfsa rm *.swp" as less syntactically demanding "find -name *.swp -exec rm {} \;"
- keyboard shortcut to change kitty text size and reload all kitty terminals
- set up fzf/fdfind defaults
- look into kitty history for preexec
