#!/bin/bash

if [ $# -eq 0 ] || [ $1 = "leo" ] ; then
	popeaddress="${HOME}/img/pope.jpg"
elif [ $1 = "francis" ]; then
	popeaddress="${HOME}/img/pope2.jpg"
else
	popeaddress="${HOME}/img/what.png"
fi

eog "$popeaddress" &

killme=$!
sleep 1.5

kill -9 $killme
